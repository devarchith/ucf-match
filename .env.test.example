# Dedicated test database only. Must not point to dev/prod data.
# Name should include "test" (e.g., ucf_match_test) to satisfy safety checks.
TEST_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ucf_match_test?schema=public"
